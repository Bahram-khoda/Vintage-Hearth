sqlite3 bikes.db

CREATE TABLE bikes (
    id        TEXT           NOT NULL,
    name      TEXT           NOT NULL,
    wheels    INTEGER,
    size      INTEGER,
    motor     INTEGER (0, 1),
    folding   INTEGER (0, 1),
    image     TEXT,
    available INTEGER
);

.mode csv
.import dev/bikes.csv bikes
